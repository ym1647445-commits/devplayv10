import { ArrowRight } from "lucide-react";import Link from "next/link";import{redirect}from"next/navigation";import{AppShell}from"@/components/layout/AppShell";import{ReceiveBalanceCard}from"@/components/wallet/ReceiveBalanceCard";import{createClient}from"@/lib/supabase/server";
export default async function ReceivePage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth");const{data:p}=await s.from("profiles").select("customer_id").eq("id",user.id).single<{customer_id:string}>();if(!p)redirect("/account");return <AppShell><section className="wallet-page"><header className="wallet-heading"><div><span>رابطك الشخصي</span><h1>استلام رصيد</h1></div><Link className="wallet-history-button" href="/wallet"><ArrowRight size={17}/>رجوع</Link></header><ReceiveBalanceCard customerId={p.customer_id}/></section></AppShell>}
